version: "3.9"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    restart: unless-stopped
    environment:
      - ZB_API_KEY=${ZB_API_KEY}
      - NB_API_KEY=${NB_API_KEY}